---
export const prerender = false;

import WorldLayout from '../../layouts/WorldLayout.astro';

const path = Astro.params.path?.join('/') || '.';
---

<WorldLayout title="World File Explorer" path={path}>
  <!-- Components will be loaded via client-side JavaScript -->
</WorldLayout>